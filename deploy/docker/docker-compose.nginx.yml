version: "3.9"

services:
  nginx:
    image: nginx:latest
    container_name: my-proxy-nginx
    ports:
      - "8443:443" # 本地8443端口转到容器443
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ../cert:/etc/nginx/certs:ro
    restart: always
